function submitDesignProject() {
    // Add logic to handle design project submission
    alert("Design project submitted! Await feedback.");
}

function navigatelesson(direction) {
    if (direction === 'Next') {
        window.location.href = 'advancedcss.html';
    }
    else if (direction === 'Previous') {
        window.location.href = 'introduction.html';
    }
}